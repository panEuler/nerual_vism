Legacy configs and historical templates that are not part of the current
training/inference path live here.

- `experiment/`: older experiment/model templates kept for reference
- `loss/`: older standalone loss snippets kept for reference

Prefer the current configs under `configs/experiment/`, `configs/loss/`,
`configs/model/`, and `configs/train/` for active development.

